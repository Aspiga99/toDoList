import { mount } from '@vue/test-utils'
import Component from '../components/Profile.vue'

describe("Component", () => {
  it("renders correctly", () => {
    expect(true).toBe(true);
  });
});
