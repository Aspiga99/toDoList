import { mount } from '@vue/test-utils'
import Component from '../components/ResetPassword.vue'

describe("Component", () => {
  it("renders correctly", () => {
    expect(true).toBe(true);
  });
});
